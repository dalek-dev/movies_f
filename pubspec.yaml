name: movies
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^8.1.2 # for state manager
  bloc: ^8.1.1
 
  
  injectable: ^2.1.0
  get_it: ^7.2.0 # to access objects
  dartz: ^0.10.1 # functional

  http: ^0.13.5 # connect to cloud services
  
  json_serializable: ^6.6.1
  json_annotation: ^4.8.0

  shimmer: ^2.0.0
  scroll_snap_list: ^0.8.1

  envied: ^0.3.0

  equatable: ^2.0.5

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  envied_generator: ^0.3.0
  injectable_generator: ^2.1.4
  build_runner: ^2.3.3

  flutter_lints: ^2.0.0


flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/clips/

